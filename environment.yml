# Conda environment (alternative to `pip install -r requirements-dev.txt`).
#   conda env create -f environment.yml
#   conda activate delivery-delay
name: delivery-delay
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r requirements-dev.txt
